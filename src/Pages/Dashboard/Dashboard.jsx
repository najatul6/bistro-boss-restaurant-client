import { Helmet } from "react-helmet-async";

const Dashboard = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro Boss | Home</title>
            </Helmet>
            DashBoard
        </div>
    );
};

export default Dashboard;