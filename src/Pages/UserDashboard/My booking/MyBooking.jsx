import SectionTitle from "../../../Components/SectionTitle/SectionTitle";

const MyBooking = () => {
    return (
        <div>
            <SectionTitle subHeading="booking" heading="booking"></SectionTitle>
        </div>
    );
};

export default MyBooking;