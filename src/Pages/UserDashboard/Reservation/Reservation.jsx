import SectionTitle from "../../../Components/SectionTitle/SectionTitle";

const Reservation = () => {
    return (
        <div>
            <SectionTitle subHeading="reservation" heading="Reservation"></SectionTitle>
        </div>
    );
};

export default Reservation;