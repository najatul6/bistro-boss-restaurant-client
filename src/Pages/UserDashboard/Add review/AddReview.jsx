import SectionTitle from "../../../Components/SectionTitle/SectionTitle";

const AddReview = () => {
    return (
        <div>
            <SectionTitle subHeading="reviews" heading="reviews"></SectionTitle>
        </div>
    );
};

export default AddReview;